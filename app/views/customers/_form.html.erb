<%= form_for @customer do |f| %>
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </p>

    <p>
      <%= f.submit %>
    </p>

    <p>
      <%= link_to 'Back', customers_path, class: "btn btn-default" %>
    </p>

<% end %>